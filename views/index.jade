doctype 5
html(lang="en")
  head
    title Voxel Mars
    meta(charset="utf-8")
    style(type="text/css").
      html, body {margin: 0; padding: 0;}
      #pointer {
        position: absolute;
        top: 0; left: 0;
        width: 3px; height: 3px;
        background-color: #f00;
      }
      #mid-map {
        display: none;
      }
      #mid-map img {width: 100%;}
    script(src="/js/zepto-1.1.1.min.js")
    script.
      window.app = { map: !{json(map, { except: 'data' })} };
    script(src="/js/index.js")
  body
    div(id="mid-map")
      div(id="pointer")
      img(src="/maps/#{map.name}-mid.jpg")